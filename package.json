{
    "dependencies": {
        "esbuild": "^0.18.10"
    },
    "scripts": {
        "build": "esbuild ./src/index.js --bundle --platform=neutral --outfile=dist/brightspace-assessment-scraper.js"
    }
}
